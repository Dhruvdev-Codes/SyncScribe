feat: Add developer activity tracking, database audit logging, and AI fallback resilience

- Add ActivityLog, ProjectMetric, DevActivity models to Prisma schema
- Create activityLogger service with logActivity() and logDevActivity()
- Add devController endpoints: /api/dev/activities, /api/dev/tasks, /api/dev/stats, /api/dev/details, /api/dev/export
- Wire devRoutes into main API router under /api/dev
- Integrate activity logging across controllers:
  documentController: DOCUMENT_CREATED, DOCUMENT_UPDATED, DOCUMENT_DELETED, DOCUMENT_DUPLICATED
  versionController: SNAPSHOT_CREATED, VERSION_RESTORED
  commentController: COMMENT_CREATED, COMMENT_RESOLVED, COMMENT_REOPENED
  aiController: AI_GENERATION, AI_REWRITE, AI_SUMMARIZE, AI_TRANSLATE
  socketManager: COLLABORATOR_JOINED, CHAT_MESSAGE_SENT
- Fix documentController createDocument scope bug (logActivity inside try)
- Keep aiController local-engine fallback for all AI endpoints
- Clean TypeScript build passes (tsc succeeds)