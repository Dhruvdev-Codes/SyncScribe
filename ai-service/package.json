{
  "name": "syncscribe-ai-service",
  "version": "1.0.0",
  "description": "Python FastAPI AI microservice for SyncScribe",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000 --host 0.0.0.0",
    "start": "python -m uvicorn app.main:app --port 8000 --host 0.0.0.0"
  }
}
